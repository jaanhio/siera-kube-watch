{
  "name": "siera-kube-watch",
  "version": "1.0.0",
  "description": "**siera kubewatch** is a Kubernetes events watcher that aims to publish incident (unexpected event) as a notification through webhooks.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jaanhio/siera-kube-watch.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jaanhio/siera-kube-watch/issues"
  },
  "homepage": "https://github.com/jaanhio/siera-kube-watch#readme",
  "devDependencies": {
    "semantic-release": "^17.4.4"
  },
  "release": {
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/github"
    ]
  }
}
